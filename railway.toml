[build]
builder = "nixpacks"

[deploy]
startCommand = "cd comsigns-backend && python3 -m uvicorn backend.api.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
