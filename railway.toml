[build]
builder = "nixpacks"

[deploy]
startCommand = "cd comsigns-backend && pip install -r requirements.txt && uvicorn backend.api.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
